import type React from "react"
import "./globals.css"

export const metadata = {
  title: "Sendcloud Case Studies",
  description: "Scopri come i nostri clienti hanno ottenuto risultati straordinari con Sendcloud",
  generator: "v0.dev",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
